#include "main.h"
/**
 * print_sign - prints the sign of a number.
 * @n: is the number
 * Return: 1 if n > 0, 0 if n = 0, -1 if n < 0
 */
int print_sign(int n)
{
	for (n >= 0;i)
	{
		for (n = 0;)
		{
			return (0);
		else
			return (1);
		}
	else
		return (-1);
	}
}
